"""Operational scripts — seeding reference data, one-off maintenance.

These are exposed as `python -m scripts.<name>` modules so they're importable
from tests. CLAUDE.md classifies this as "operational scripts"; the package
shape exists for test reuse, not as a public surface.
"""
